<h2>Lista de Postagens</h2>
<hr>
<a href="/admin/postagens/add"><button class="btn btn-success" >Nova de postagen</button></a>
<br>
<br>
{{#each postagens}}
    <div class="card mt-4>
        <div class="card body">
            <h4>Titulo: {{titulo}}</h4>
            <p>Descrição: {{descricao}}</p>
            <small>Data: {{data}}</small><br>
            <small>Categoria: {{categoria.nome}}</small>
        </div>
    </div>

{{else}}
<br>
<p> Nenhuma postagem registrada!!</p>
{{/each}}